name: Create GitHub release (examples repo)

on:
  push:
    tags:
      - v*

jobs:
  create_release:
    runs-on: ubuntu-22.04
    permissions:
      contents: write # create the GH release
    steps:
      - uses: actions/checkout@v4
      - name: Set env
        run: |
          echo "RELEASE_VERSION=${GITHUB_REF#refs/*/}" >> $GITHUB_ENV
      - name: Create release
        uses: ncipollo/release-action@v1
        with:
          body: |
            Examples for [bpmn-visualization@${{ env.RELEASE_VERSION }}](https://github.com/process-analytics/bpmn-visualization-js/releases/tag/v${{ env.RELEASE_VERSION }}).
            Live examples are available for [${{ env.RELEASE_VERSION }}](https://cdn.statically.io/gh/process-analytics/bpmn-visualization-examples/v${{ env.RELEASE_VERSION }}/examples/index.html).
          draft: true
          generateReleaseNotes: true
          name: ${{ env.RELEASE_VERSION }}
